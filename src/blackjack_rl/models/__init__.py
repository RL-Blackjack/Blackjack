"""이 파일은 지도학습·모방학습 모델 묶음의 공개 API를 모아 둔다.
입력: 없음(패키지를 import할 때 실행된다).
출력: 특징 인코딩·분할·학습·레지스트리·비교 함수들의 재수출.
"""

# 왜 여기서 하위 모듈을 import하지 않는가: features만 필요한 곳이 sklearn까지
#   끌고 오게 되어 pytest가 느려진다. 쓰는 쪽이 필요한 모듈을 직접 import한다.

__all__: list[str] = []
