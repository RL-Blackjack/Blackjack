# W1~W8 범위에서 실제로 쓰는 것만 적는다.
# 왜: torch는 계획서 ⑤(DQN), fastapi·psycopg는 계획서 ④(게임 서버)에서 추가한다.
#     지금 넣으면 "이 파일 왜 있나"에 답할 수 없는 의존성이 된다.
numpy==2.4.3
pandas==3.0.1
pytest==9.0.2

# W4 시각화: 정적 PNG와 GIF 전용. pillow는 matplotlib의 PillowWriter가 쓴다.
matplotlib==3.11.2
pillow==12.3.0

# W7 지도학습 비교
scikit-learn==1.9.1
joblib==1.6.0

# W8 연구 대시보드. app/ 안에서만 쓰고 src/ 에서는 import하지 않는다.
streamlit==1.64.0
plotly==7.1.0

# W9 게임 서버. game/ 안에서만 쓰고 src/ 에서는 import하지 않는다.
fastapi==0.141.1
uvicorn==0.53.0
sqlalchemy==2.0.54
argon2-cffi==25.1.0
pyjwt==2.14.0
httpx==0.28.1
pytest-asyncio==1.4.0
# 왜: pydantic의 EmailStr은 이 패키지가 없으면 ImportError를 낸다.
email-validator==2.3.0

# W10 게임 프런트·배포. game/ 과 deploy/ 에서만 쓴다.
# 왜 cryptography인가: 구글 ID 토큰은 RS256 서명이라 PyJWT가 이 패키지를 요구한다.
cryptography==50.0.1
# 왜 psycopg 3인가: 배포 DB 드라이버를 하나로 고정한다(BJ_ENV=prod 는 postgresql+psycopg:// 만 받는다).
psycopg[binary]==3.3.6
# 왜 pyyaml인가: 배포 compose 파일을 테스트가 읽어 보안 규칙(포트 비공개 등)을 고정한다.
pyyaml==6.0.3
