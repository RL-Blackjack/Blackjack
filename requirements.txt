# W1~W8 범위에서 실제로 쓰는 것만 적는다.
# 왜: torch는 계획서 ⑤(DQN), fastapi·psycopg는 계획서 ④(게임 서버)에서 추가한다.
#     지금 넣으면 "이 파일 왜 있나"에 답할 수 없는 의존성이 된다.
numpy==2.4.3
pandas==3.0.1
pytest==9.0.2

# W4 시각화: 정적 PNG와 GIF 전용. pillow는 matplotlib의 PillowWriter가 쓴다.
matplotlib==3.11.2
pillow==12.3.0

# W7 지도학습 비교
scikit-learn==1.9.1
joblib==1.6.0

# W8 연구 대시보드. app/ 안에서만 쓰고 src/ 에서는 import하지 않는다.
streamlit==1.64.0
plotly==7.1.0
